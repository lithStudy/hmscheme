# 膳食规划系统目标评估体系说明文档

## 1. 目标系统概述

### 1.1 设计理念
膳食规划系统采用四层目标评估体系，从营养充足性、营养平衡性、食物质量和用户适配性四个维度全面评估膳食方案的优劣。每个目标都具有丰富的可配置参数，支持个性化定制和精细调优。

### 1.2 目标层次结构
```
MultiObjectiveEvaluator (多目标评估器)
├── NutrientTargetObjective[] (营养素目标群) - 10个目标
├── NutrientBalanceObjective (营养平衡目标) - 1个目标  
├── FoodQualityObjective (食物质量目标) - 1个目标
└── UserPreferenceObjective (用户偏好目标) - 1个目标
```

## 2. 营养素目标群 (NutrientTargetObjective)

### 2.1 目标描述
评估单个营养素的达成情况，确保用户获得充足的各类营养素。

### 2.2 可变参数

#### 2.2.1 营养素类型配置
```java
// 主要营养素 (权重较高)
public static final NutrientType[] mainNutrients = {
    NutrientType.CALORIES,        // 热量
    NutrientType.PROTEIN,         // 蛋白质  
    NutrientType.FAT,            // 脂肪
    NutrientType.CARBOHYDRATES,  // 碳水化合物
    NutrientType.FIBER           // 纤维
};

// 微量营养素 (权重较低)
public static final NutrientType[] microNutrients = {
    NutrientType.CALCIUM,        // 钙
    NutrientType.IRON,          // 铁
    NutrientType.VITAMIN_A,     // 维生素A
    NutrientType.VITAMIN_C,     // 维生素C  
    NutrientType.SODIUM         // 钠
};
```

#### 2.2.2 权重参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `mainNutrientWeight` | 0.06 | 0.01-0.15 | 主要营养素权重 |
| `microNutrientWeight` | 0.02 | 0.005-0.08 | 微量营养素权重 |

#### 2.2.3 评估参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `optimalRatio` | 1.0 | 固定 | 理想达成比例 |
| `penaltyFactor` | 0.5 | 0.1-1.0 | 不足/过量惩罚系数 |
| `excessPenaltyRate` | 0.3 | 0.1-0.8 | 过量额外惩罚率 |

### 2.3 评估算法
```java
double ratio = actualValue / targetValue;
double[] range = NutrientType.getNutrientRates(userProfile, nutrientType);
double minRate = range[0];  // 最低达成率 (如0.8)
double maxRate = range[1];  // 最高达成率 (如1.5)

if (ratio >= minRate && ratio <= maxRate) {
    // 在合理范围内，越接近1.0得分越高
    score = 1.0 - Math.abs(ratio - 1.0) * penaltyFactor;
} else if (ratio < minRate) {
    // 不足惩罚
    score = ratio / minRate * penaltyFactor;
} else {
    // 过量惩罚  
    double excess = (ratio - maxRate) / maxRate;
    score = Math.max(0.0, penaltyFactor - excess * excessPenaltyRate);
}
```

### 2.4 个性化配置
```java
// 根据用户健康状况调整营养素权重
public void adjustWeightsForHealthCondition(HealthConditionType condition) {
    switch (condition) {
        case DIABETES:
            // 糖尿病患者增加纤维权重，降低碳水权重
            setNutrientWeight(NutrientType.FIBER, 0.10);
            setNutrientWeight(NutrientType.CARBOHYDRATES, 0.04);
            break;
        case HYPERTENSION:
            // 高血压患者增加钠的监控权重
            setNutrientWeight(NutrientType.SODIUM, 0.08);
            break;
        case ANEMIA:
            // 贫血患者增加铁的权重
            setNutrientWeight(NutrientType.IRON, 0.06);
            break;
    }
}
```

## 3. 营养平衡目标 (NutrientBalanceObjective)

### 3.1 目标描述
评估膳食的营养结构平衡性，包括宏量营养素比例、热量合理性和食物分量适宜性。

### 3.2 可变参数

#### 3.2.1 维度权重配置
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `macroRatioWeight` | 0.4 | 0.2-0.6 | 宏量营养素比例权重 |
| `calorieRationalityWeight` | 0.4 | 0.2-0.6 | 热量合理性权重 |
| `portionRationalityWeight` | 0.2 | 0.1-0.4 | 食物分量合理性权重 |

#### 3.2.2 宏量营养素理想比例
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `idealCarbRatio` | 0.55 | 0.45-0.65 | 碳水化合物热量占比 |
| `idealProteinRatio` | 0.20 | 0.15-0.25 | 蛋白质热量占比 |
| `idealFatRatio` | 0.25 | 0.20-0.35 | 脂肪热量占比 |
| `ratioTolerance` | 0.05 | 0.02-0.10 | 比例容差 |

#### 3.2.3 热量合理性参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `calorieToleranceRange` | 0.15 | 0.05-0.25 | 热量容差范围 |
| `severeDeviationThreshold` | 0.30 | 0.20-0.50 | 严重偏离阈值 |
| `moderateDeviationPenalty` | 0.5 | 0.2-0.8 | 中度偏离惩罚 |
| `severeDeviationPenalty` | 0.8 | 0.5-1.0 | 严重偏离惩罚 |

#### 3.2.4 食物分量参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `minPortionSize` | 20.0 | 10.0-50.0 | 最小分量(g) |
| `maxPortionSize` | 300.0 | 200.0-500.0 | 最大分量(g) |
| `idealPortionRange` | [50.0, 200.0] | 可调 | 理想分量范围 |
| `portionPenaltyRate` | 0.3 | 0.1-0.6 | 分量不合理惩罚率 |

### 3.3 评估算法

#### 3.3.1 宏量营养素比例评估
```java
double carbRatio = carbCalories / totalCalories;
double proteinRatio = proteinCalories / totalCalories;  
double fatRatio = fatCalories / totalCalories;

double carbScore = calculateRatioScore(carbRatio, idealCarbRatio, ratioTolerance);
double proteinScore = calculateRatioScore(proteinRatio, idealProteinRatio, ratioTolerance);
double fatScore = calculateRatioScore(fatRatio, idealFatRatio, ratioTolerance);

double macroScore = (carbScore + proteinScore + fatScore) / 3.0;
```

#### 3.3.2 热量合理性评估
```java
double calorieDeviation = Math.abs(actualCalories - targetCalories) / targetCalories;

if (calorieDeviation <= calorieToleranceRange) {
    calorieScore = 1.0;
} else if (calorieDeviation <= severeDeviationThreshold) {
    calorieScore = 1.0 - (calorieDeviation - calorieToleranceRange) * moderateDeviationPenalty;
} else {
    calorieScore = 1.0 - severeDeviationPenalty;
}
```

### 3.4 个性化配置
```java
// 根据用户活动水平调整热量容差
public void adjustForActivityLevel(double activityLevel) {
    if (activityLevel >= 1.8) { // 高强度运动
        calorieToleranceRange = 0.20;
    } else if (activityLevel <= 1.3) { // 久坐
        calorieToleranceRange = 0.10;
    }
}

// 根据年龄调整宏量营养素比例
public void adjustForAge(int age) {
    if (age >= 65) {
        idealProteinRatio = 0.25; // 老年人增加蛋白质比例
        idealCarbRatio = 0.50;
    } else if (age <= 18) {
        idealCarbRatio = 0.60; // 青少年增加碳水比例
        idealProteinRatio = 0.18;
    }
}
```

## 4. 食物质量目标 (FoodQualityObjective)

### 4.1 目标描述
评估食物搭配的质量，包括类别多样性、营养完整性和适度的特性多样性。

### 4.2 可变参数

#### 4.2.1 维度权重配置
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `categoryDiversityWeight` | 0.4 | 0.2-0.6 | 类别多样性权重 |
| `nutritionCompletenessWeight` | 0.4 | 0.2-0.6 | 营养完整性权重 |
| `varietyWeight` | 0.2 | 0.1-0.4 | 适度多样性权重 |

#### 4.2.2 理想类别分布
| 食物类别 | 理想占比 | 可调范围 | 说明 |
|---------|---------|---------|------|
| `STAPLE` | 0.35 | 0.25-0.45 | 主食类 |
| `VEGETABLE` | 0.30 | 0.20-0.40 | 蔬菜类 |
| `PROTEIN` | 0.20 | 0.15-0.30 | 蛋白质类 |
| `FRUIT` | 0.10 | 0.05-0.20 | 水果类 |
| `DAIRY` | 0.05 | 0.02-0.15 | 乳制品类 |

#### 4.2.3 营养完整性参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `requiredCategories` | 3 | 2-5 | 必需类别数 |
| `bonusCategoryThreshold` | 4 | 3-6 | 额外奖励类别阈值 |
| `missingCategoryPenalty` | 0.3 | 0.1-0.5 | 缺失类别惩罚 |
| `bonusPoints` | 0.1 | 0.05-0.2 | 额外类别奖励 |

#### 4.2.4 多样性控制参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `maxFoodRepetition` | 1 | 1-2 | 最大食物重复次数 |
| `repetitionPenalty` | 0.2 | 0.1-0.4 | 重复惩罚系数 |
| `varietyBonus` | 0.05 | 0.02-0.1 | 多样性奖励 |

### 4.3 评估算法

#### 4.3.1 类别多样性评估
```java
Map<FoodCategory, Double> actualDistribution = calculateCategoryDistribution(solution);
double diversityScore = 0.0;

for (FoodCategory category : FoodCategory.values()) {
    double actual = actualDistribution.getOrDefault(category, 0.0);
    double ideal = idealDistribution.get(category);
    
    // 使用余弦相似度计算分布相似性
    diversityScore += Math.sqrt(actual * ideal);
}

// 应用类别覆盖奖励
int coveredCategories = (int) actualDistribution.keySet().size();
if (coveredCategories >= bonusCategoryThreshold) {
    diversityScore += bonusPoints * (coveredCategories - bonusCategoryThreshold + 1);
}
```

#### 4.3.2 营养完整性评估
```java
Set<FoodCategory> presentCategories = solution.getFoodGenes().stream()
    .map(gene -> gene.getFood().getCategory())
    .collect(Collectors.toSet());

double completenessScore = 1.0;
int missingCount = Math.max(0, requiredCategories - presentCategories.size());
completenessScore -= missingCount * missingCategoryPenalty;

// 检查关键营养组合
if (hasCompleteProteinSources(solution) && hasCompleteVitaminSources(solution)) {
    completenessScore += varietyBonus;
}
```

### 4.4 个性化配置
```java
// 根据饮食偏好调整类别分布
public void adjustForDietaryPreference(DietaryPreference preference) {
    switch (preference) {
        case VEGETARIAN:
            idealDistribution.put(FoodCategory.PROTEIN, 0.15);
            idealDistribution.put(FoodCategory.VEGETABLE, 0.35);
            idealDistribution.put(FoodCategory.DAIRY, 0.10);
            break;
        case LOW_CARB:
            idealDistribution.put(FoodCategory.STAPLE, 0.20);
            idealDistribution.put(FoodCategory.PROTEIN, 0.30);
            idealDistribution.put(FoodCategory.VEGETABLE, 0.35);
            break;
        case MEDITERRANEAN:
            idealDistribution.put(FoodCategory.FRUIT, 0.15);
            idealDistribution.put(FoodCategory.OIL, 0.08);
            break;
    }
}
```

## 5. 用户偏好目标 (UserPreferenceObjective)

### 5.1 目标描述
评估膳食方案对用户个人偏好的适配程度，包括安全性、口味偏好和口味多样性。

### 5.2 可变参数

#### 5.2.1 维度权重配置
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `safetyWeight` | 0.6 | 0.4-0.8 | 安全性权重(硬约束) |
| `flavorMatchWeight` | 0.25 | 0.1-0.4 | 口味匹配权重 |
| `flavorDiversityWeight` | 0.15 | 0.1-0.3 | 口味多样性权重 |

#### 5.2.2 安全性参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `allergenViolationPenalty` | 1.0 | 固定 | 过敏原违规惩罚 |
| `religiousViolationPenalty` | 1.0 | 固定 | 宗教禁忌违规惩罚 |
| `safetyThreshold` | 1.0 | 固定 | 安全性阈值 |
| `isHardConstraint` | true | 固定 | 是否为硬约束 |

#### 5.2.3 口味偏好参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `preferredFlavorBonus` | 0.3 | 0.1-0.5 | 偏好口味奖励 |
| `dislikedFlavorPenalty` | 0.4 | 0.2-0.6 | 不喜欢口味惩罚 |
| `neutralFlavorScore` | 0.5 | 0.3-0.7 | 中性口味得分 |
| `spicyToleranceLevel` | 3 | 1-5 | 辣度容忍等级 |

#### 5.2.4 口味多样性参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `minFlavorVariety` | 2 | 1-4 | 最少口味种类 |
| `optimalFlavorVariety` | 4 | 3-6 | 最佳口味种类 |
| `varietyBonusRate` | 0.1 | 0.05-0.2 | 多样性奖励率 |
| `monotonyPenalty` | 0.3 | 0.1-0.5 | 单调惩罚 |

### 5.3 评估算法

#### 5.3.1 安全性评估 (硬约束)
```java
boolean isSafe = true;
double safetyScore = 1.0;

// 检查过敏原
for (FoodGene gene : solution.getFoodGenes()) {
    Food food = gene.getFood();
    if (hasAllergenConflict(food, userProfile.getAllergies())) {
        isSafe = false;
        safetyScore = 0.0;
        break;
    }
}

// 检查宗教禁忌
if (isSafe && hasReligiousConflict(solution, userProfile.getReligiousRestrictions())) {
    isSafe = false;
    safetyScore = 0.0;
}

return new ObjectiveValue(getName(), safetyScore, getWeight(), isSafe);
```

#### 5.3.2 口味匹配评估
```java
double totalFlavorScore = 0.0;
int foodCount = 0;

for (FoodGene gene : solution.getFoodGenes()) {
    Food food = gene.getFood();
    String[] flavors = food.getFlavors();
    
    double foodFlavorScore = neutralFlavorScore; // 默认中性得分
    
    for (String flavor : flavors) {
        if (userProfile.getLikedFlavors().contains(flavor)) {
            foodFlavorScore += preferredFlavorBonus;
        } else if (userProfile.getDislikedFlavors().contains(flavor)) {
            foodFlavorScore -= dislikedFlavorPenalty;
        }
    }
    
    // 检查辣度匹配
    if (food.getSpicyLevel() > userProfile.getSpicyTolerance()) {
        foodFlavorScore -= (food.getSpicyLevel() - userProfile.getSpicyTolerance()) * 0.1;
    }
    
    totalFlavorScore += Math.max(0.0, Math.min(1.0, foodFlavorScore));
    foodCount++;
}

return foodCount > 0 ? totalFlavorScore / foodCount : 0.0;
```

#### 5.3.3 口味多样性评估
```java
Set<String> uniqueFlavors = solution.getFoodGenes().stream()
    .flatMap(gene -> Arrays.stream(gene.getFood().getFlavors()))
    .collect(Collectors.toSet());

int flavorCount = uniqueFlavors.size();
double diversityScore = 0.5; // 基础得分

if (flavorCount < minFlavorVariety) {
    diversityScore -= (minFlavorVariety - flavorCount) * monotonyPenalty;
} else if (flavorCount >= optimalFlavorVariety) {
    diversityScore += (flavorCount - optimalFlavorVariety + 1) * varietyBonusRate;
}

return Math.max(0.0, Math.min(1.0, diversityScore));
```

### 5.4 个性化配置
```java
// 根据文化背景调整偏好
public void adjustForCulturalBackground(CulturalBackground background) {
    switch (background) {
        case CHINESE:
            addPreferredFlavors("鲜", "香", "嫩");
            setSpicyToleranceLevel(3);
            break;
        case WESTERN:
            addPreferredFlavors("甜", "奶香", "果香");
            setSpicyToleranceLevel(2);
            break;
        case INDIAN:
            addPreferredFlavors("辛辣", "香料", "浓郁");
            setSpicyToleranceLevel(5);
            break;
    }
}

// 根据年龄调整口味偏好
public void adjustForAge(int age) {
    if (age >= 65) {
        // 老年人偏好清淡口味
        setSpicyToleranceLevel(2);
        addPreferredFlavors("清淡", "鲜");
        increaseFlavorMatchWeight(0.05);
    } else if (age <= 25) {
        // 年轻人更能接受多样化口味
        increaseFlavorDiversityWeight(0.05);
        setSpicyToleranceLevel(4);
    }
}
```

## 6. 多目标评估器 (MultiObjectiveEvaluator)

### 6.1 全局配置参数

#### 6.1.1 质量阈值参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `goodEnoughThreshold` | 0.8 | 0.6-0.95 | 解决方案质量阈值 |
| `excellentThreshold` | 0.9 | 0.8-0.98 | 优秀解决方案阈值 |
| `hardConstraintWeight` | 2.0 | 1.5-3.0 | 硬约束权重放大系数 |

#### 6.1.2 权重归一化参数
| 参数名 | 默认值 | 可调范围 | 说明 |
|-------|-------|---------|------|
| `enableWeightNormalization` | true | true/false | 是否启用权重归一化 |
| `totalWeightTarget` | 1.0 | 0.8-1.2 | 目标总权重 |
| `minObjectiveWeight` | 0.01 | 0.005-0.05 | 最小目标权重 |

### 6.2 动态权重调整
```java
// 根据用户重要性偏好动态调整权重
public void customizeWeights(UserImportanceProfile importance) {
    // 营养素目标权重调整
    if (importance.getNutritionImportance() == ImportanceLevel.HIGH) {
        multiplyNutrientWeights(1.2);
    } else if (importance.getNutritionImportance() == ImportanceLevel.LOW) {
        multiplyNutrientWeights(0.8);
    }
    
    // 平衡目标权重调整
    balanceObjective.setWeight(0.3 * importance.getBalanceImportance().getMultiplier());
    
    // 质量目标权重调整
    foodQualityObjective.setWeight(0.2 * importance.getQualityImportance().getMultiplier());
    
    // 偏好目标权重调整
    userPreferenceObjective.setWeight(0.2 * importance.getPreferenceImportance().getMultiplier());
}
```

### 6.3 综合评估算法
```java
public double calculateOverallScore(MealSolution solution) {
    double totalScore = 0.0;
    double totalWeight = 0.0;
    boolean hardConstraintViolated = false;
    
    for (ObjectiveEvaluator objective : objectives) {
        ObjectiveValue value = objective.evaluate(solution, targetNutrients);
        double weight = objective.getWeight();
        
        // 硬约束检查
        if (objective.isHardConstraint() && 
            value.getValue() < objective.getHardConstraintThreshold()) {
            hardConstraintViolated = true;
            weight *= hardConstraintWeight; // 放大硬约束权重
        }
        
        totalScore += value.getValue() * weight;
        totalWeight += weight;
    }
    
    double score = totalWeight > 0 ? totalScore / totalWeight : 0.0;
    
    // 硬约束违规严重惩罚
    if (hardConstraintViolated) {
        score *= 0.5;
    }
    
    return Math.max(0.0, Math.min(1.0, score));
}
```

## 7. 配置示例

### 7.1 糖尿病患者配置
```java
MultiObjectiveEvaluator evaluator = new MultiObjectiveEvaluator(diabeticProfile);

// 调整营养素权重
evaluator.setNutrientWeight(NutrientType.FIBER, 0.10);
evaluator.setNutrientWeight(NutrientType.CARBOHYDRATES, 0.04);
evaluator.setNutrientWeight(NutrientType.SODIUM, 0.06);

// 调整平衡目标参数
evaluator.getBalanceObjective().setIdealCarbRatio(0.45);
evaluator.getBalanceObjective().setCalorieToleranceRange(0.10);

// 调整食物质量参数
evaluator.getFoodQualityObjective().adjustIdealDistribution(
    FoodCategory.VEGETABLE, 0.40);
evaluator.getFoodQualityObjective().adjustIdealDistribution(
    FoodCategory.STAPLE, 0.25);

// 设置更严格的质量阈值
evaluator.setGoodEnoughThreshold(0.85);
```

### 7.2 健身人群配置
```java
MultiObjectiveEvaluator evaluator = new MultiObjectiveEvaluator(athleteProfile);

// 增加蛋白质权重
evaluator.setNutrientWeight(NutrientType.PROTEIN, 0.10);
evaluator.setNutrientWeight(NutrientType.CALORIES, 0.08);

// 调整宏量营养素比例
evaluator.getBalanceObjective().setIdealProteinRatio(0.25);
evaluator.getBalanceObjective().setIdealCarbRatio(0.50);
evaluator.getBalanceObjective().setCalorieToleranceRange(0.20);

// 增加蛋白质类食物比例
evaluator.getFoodQualityObjective().adjustIdealDistribution(
    FoodCategory.PROTEIN, 0.30);

// 降低质量阈值，允许更大的热量范围
evaluator.setGoodEnoughThreshold(0.75);
```

### 7.3 儿童配置
```java
MultiObjectiveEvaluator evaluator = new MultiObjectiveEvaluator(childProfile);

// 增加钙和维生素权重
evaluator.setNutrientWeight(NutrientType.CALCIUM, 0.06);
evaluator.setNutrientWeight(NutrientType.VITAMIN_A, 0.04);
evaluator.setNutrientWeight(NutrientType.VITAMIN_C, 0.04);

// 调整宏量营养素比例，增加碳水化合物
evaluator.getBalanceObjective().setIdealCarbRatio(0.60);
evaluator.getBalanceObjective().setIdealProteinRatio(0.18);

// 增加乳制品和水果比例
evaluator.getFoodQualityObjective().adjustIdealDistribution(
    FoodCategory.DAIRY, 0.15);
evaluator.getFoodQualityObjective().adjustIdealDistribution(
    FoodCategory.FRUIT, 0.15);

// 降低辣度容忍度
evaluator.getUserPreferenceObjective().setSpicyToleranceLevel(1);
```

## 8. 参数调优建议

### 8.1 权重调优策略
1. **营养素权重**: 根据用户健康状况和营养需求重点调整
2. **平衡权重**: 对于有特殊饮食要求的用户可适当提高
3. **质量权重**: 对于注重食物品质的用户可适当提高
4. **偏好权重**: 对于有强烈个人偏好的用户可适当提高

### 8.2 阈值调优策略
1. **质量阈值**: 根据用户对解决方案质量的要求调整
2. **容差范围**: 根据用户的灵活性需求调整
3. **惩罚系数**: 根据违规行为的严重程度调整

### 8.3 个性化调优流程
1. **用户画像分析**: 分析用户的健康状况、饮食偏好、生活方式
2. **参数初始化**: 基于用户画像设置初始参数
3. **反馈优化**: 根据用户对推荐结果的反馈调整参数
4. **持续学习**: 基于用户的长期使用数据优化参数

这套目标评估体系通过丰富的可配置参数，能够灵活适应不同用户的个性化需求，为膳食规划提供精确、科学的评估标准。
